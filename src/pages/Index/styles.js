import styled from "styled-components";

export const TrendVideo = styled.section`
  & {
    padding: var(--gap-xl) 0;
  }
`;

export const IndexContainer = styled.main`
  & {
    background-color: linear-gradient(
      180deg,
      var(--secondary-3) 0%,
      var(--secondary-1) 100%
    );
  }
`;

export const Videos = styled.section`
  & {
    padding: 0 0 var(--gap-xl) 0;
  }
`;
